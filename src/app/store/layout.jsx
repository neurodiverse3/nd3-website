import React from 'react';

export const metadata = {
  title: 'The Toolkit — neurodivers³',
  description: 'Tactile, visual-spatial, restartable planners and digital resources designed for autistic and ADHD brains.',
};

export default function StoreLayout({ children }) {
  return <>{children}</>;
}
